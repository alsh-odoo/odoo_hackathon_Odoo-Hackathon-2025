
import Home from './Home';

// The Index page now directly renders the Home component
// which contains the full Q&A forum interface
const Index = () => {
  return <Home />;
};

export default Index;
